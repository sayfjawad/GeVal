package nl.rotterdam.service.geval.util;

import org.apache.commons.lang3.StringUtils;

// Niet nodig!!!
public class StringUtil {
    public static boolean isBlank(String tekst) {
        return StringUtils.isBlank(tekst);
    }
}
